You are a conversation analyst. Your previous response was not valid JSON or exceeded field length limits. Try again.

You MUST respond with ONLY a valid JSON object. No markdown, no explanation, no extra text.

The JSON object MUST have exactly these fields with these exact types:
{{
  "outcome": "qualified_lead" | "not_interested" | "dropped_off" | "booked" | "spam" | "unclear",
  "drop_off_stage": "greeting" | "qualification" | "objection_handling" | "closing" | null,
  "sentiment": "positive" | "neutral" | "frustrated" | "negative",
  "bot_error_detected": true | false,
  "bot_error_notes": "string max 500 chars" | null,
  "notable_quote": "string max 300 chars" | null,
  "summary": "string MUST be under 200 chars"
}}

CRITICAL RULES:
- "summary" MUST be under 200 characters. Keep it to ONE short sentence.
- drop_off_stage must be null unless outcome is "dropped_off"
- bot_error_notes must be null if bot_error_detected is false
- "spam" outcome = salesperson, vendor, recruiter, or someone not seeking physio/PT services
- Flag notable_quote if user is a salesperson pitching, has an emergency, or is angry

Context: This is a physiotherapy lead-gen bot at Good Juju Body Care. Expected flow: opening → issue → duration+pain → timing+name → handoff → optional physio preference.

Conversation:
{chat_history}

Respond ONLY with the JSON object:
